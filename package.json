{
  "name": "fullstack-app",
  "version": "1.0.0",
  "scripts": {
    "build": "cd smart-home-control/frontend && npm install && npm run build",
    "start": "cd smart-home-control/backend && npm install && node server.js"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1"
  }
}
